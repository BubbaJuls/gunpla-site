{
  "name": "gunpla-site",
  "private": true,
  "description": "Static Gunpla shop — HTML, CSS, and vanilla JS (no build step)",
  "scripts": {
    "start": "npx serve . -l 3000",
    "dev": "npx serve . -l 3000"
  }
}
